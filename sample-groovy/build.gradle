plugins {
    id 'java'
    id 'application'
    id 'idea'
    id "io.vertx.vertx-plugin" version "0.3.1"
    id 'de.fayard.buildSrcVersions' version '0.5.0'
}

group = "de.fayard"

repositories {
    maven {
        setUrl("../plugin/src/test/resources/maven")
    }
}

dependencies {
    implementation("com.google.guava:guava:15.0")
    implementation("com.google.inject:guice:2.0")
}

vertx {
    mainVerticle = 'sample.App'
}

buildSrcVersions {
    useFdqnFor()
    renameLibs = "Libs"
    renameVersions = "Versions"
    indent = "  "
    rejectedVersionKeywords("alpha", "beta", "rc", "cr", "m", "preview", "eap", "milestone2")
}
