# This file was generated using Kotlin DSL (build.main.kts).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/krzema12/github-actions-kotlin-dsl

name: Publish to Sonatype Snapshots

on:
  workflow_dispatch:

jobs:
  "build-and-upload":
    runs-on: "ubuntu-latest"
    steps:
      - name: check-out
        uses: actions/checkout@v2
      - name: setup-java
        uses: actions/setup-java@v2
        with:
          java-version: 11
          distribution: adopt
      - name: plugins gradle publishAllPublicationsToSonatypeSnapshotsRepository
        uses: gradle/gradle-build-action@v2
        with:
          arguments: publishAllPublicationsToSonatypeSnapshotsRepository
          build-root-directory: plugins
          gradle-executable: plugins/gradlew
        env:
          sonatype_username: ${{ secrets.SONATYPE_USERNAME }}
          sonatype_password: ${{ secrets.SONATYPE_PASSWORD }}
          GPG_key_id: ${{ secrets.GPG_KEY_ID }}
          GPG_private_key: ${{ secrets.GPG_PRIVATE_KEY }}
          GPG_private_password: ${{ secrets.GPG_PRIVATE_PASSWORD }}